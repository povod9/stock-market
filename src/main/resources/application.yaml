spring:
  application:
    name: market
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: postgres
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

